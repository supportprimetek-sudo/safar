{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "sh -c 'if [ -d apps/api ]; then cd apps/api; fi && npx prisma generate && tsc'"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -d apps/api ]; then cd apps/api; fi && npx prisma db push --accept-data-loss && node dist/server.js'",
    "restartPolicyType": "ON_FAILURE"
  }
}
