{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "(cd apps/api 2>/dev/null || true) && npx prisma generate && tsc"
  },
  "deploy": {
    "startCommand": "(cd apps/api 2>/dev/null || true) && npx prisma db push --accept-data-loss && node dist/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
